export const metadata = {
  title: "Compare Job Boards Side-by-Side | JobBoardsReview",
  description: "Compare up to 4 job boards side-by-side on pricing, candidate quality, reach, and recruiter ratings to optimize your recruitment spend.",
};

export default function CompareLayout({ children }) {
  return children;
}
