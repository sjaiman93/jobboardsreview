export const metadata = {
  title: "Claim or Submit a Job Board Listing | JobBoardsReview",
  description: "Are you a job board operator or recruiter? Submit or update your job board profile, pricing tiers, and audience metrics on JobBoardsReview.",
};

export default function ClaimListingLayout({ children }) {
  return children;
}
