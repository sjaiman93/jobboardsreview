export const metadata = {
  title: "Hiring Budget Optimizer & Channel Calculator | JobBoardsReview",
  description: "Calculate expected applicants, cost-per-applicant, and optimal budget distribution across top job boards tailored to your hiring role.",
};

export default function OptimizerLayout({ children }) {
  return children;
}
