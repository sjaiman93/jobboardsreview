export const metadata = {
  title: "Job Board Directory | Explore & Filter 85+ Verified Hiring Sites",
  description: "Browse the comprehensive directory of verified job boards. Filter by industry, pricing model, and candidate reach to find the right hiring channel.",
};

export default function DirectoryLayout({ children }) {
  return children;
}
